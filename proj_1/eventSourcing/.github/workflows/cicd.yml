#setting secrets tai github
#setting - action secrets and variables - new repository secret -
#(DOCKERHUB_USERNAME, DOCKERHUB_PASSWORD, SERVER_HOST, SERVER_KEY, SERVER_PORT)
#(phongluong3366@gmail.c, ..., 103.126.161.94, id_rsa key, 22)

name: CI/CD pipeline

on:
  push:
    branches: ["main-CICD"]
  pull_request:
    branches: ["main-CICD"]

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      - name: Build and Push images
        env:
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
        run: |
          docker compose -f docker-compose-service-cicd.yml build discoverserevr
          docker compose -f docker-compose-service-cicd.yml push discoverserevr

  deploy:
    runs-on: ubuntu-latest
    needs: build-and-push

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Executing remote SSH commands
        uses: appleboy/ssh-action@v1.2.0
        env:
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_PASSWORD }}
          key: ${{ secrets.SERVER_KEY }}
          port: ${{ secrets.SERVER_PORT }}
          script_path: scripts/deploy.sh
