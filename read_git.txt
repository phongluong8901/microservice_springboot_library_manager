---
git init
git add .
git commit -m "07/08/2026"
git push

--- 
git checkout -b setup_server_client
git add .
git commit -m "setup_server_client"
git push -u origin setup_server_client

--- 
git checkout -b build_book_service
git add .
git commit -m "build_book_service CRUD"
git push -u origin build_book_service

--- 
git checkout -b p3_validation
git add .
git commit -m "add common service and validation create, get"
git push -u origin p3_validation

--- 
git checkout -b p4_employeeservice
git add .
git commit -m "add employee service: create"
git push -u origin p4_employeeservice

--- 
git checkout -b p5_swagger&apiGateway
git add .
git commit -m "change: swagger setup"
git push -u origin p5_swagger&apiGateway

--- 
git checkout -b p6_kafka_notification
git add .
git commit -m "change: first"
git push -u origin p6_kafka_notification

--- 
git checkout -b p7_borowservice_distributed_saga
git add .
git commit -m "change: first"
git push -u origin p7_borowservice_distributed_saga

--- 
git checkout -b p8_docker_microservice
git add .
git commit -m "change: first"
git push -u origin p8_docker_microservice

--- 
git checkout -b p9_keycloak_Oauth2
git add .
git commit -m "change: first"
git push -u origin p9_keycloak_Oauth2

--- 
git checkout -b p10_k8s_kurbenet
git add .
git commit -m "change: first"
git push -u origin p10_k8s_kurbenet

---
git checkout -b p11_VPS_deploy
git add .
git commit -m "change: first"
git push -u origin p11_VPS_deploy

---
git checkout -b p12_CICD_githubActions
git add .
git commit -m "change: first"
git push -u origin p12_CICD_githubActions

---
git checkout -b p13_Version_liquibase
git add .
git commit -m "change: first"
git push -u origin p13_Version_liquibase