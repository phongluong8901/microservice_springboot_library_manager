---- source
https://github.com/thanhmati/microservice-eventsourcing

--- tai lieu
https://drive.google.com/drive/folders/1utO8kufnkr5P5eASsJ4mutSn-ahA4ahl


--- run eureka server
cd proj_1
cd eventSourcing
cd discoverserver
cd discoverserver

-- install
mvn clean install

-- chay bang maven
mvn spring-boot:run

-- serverlink spring
http://localhost:8761

--- run eureka client
cd proj_1
cd eventSourcing
cd bookservice
cd bookservice

--- run eureka common
cd proj_1
cd eventSourcing
cd commonservice
cd commonservice

--- run eureka employee
cd proj_1
cd eventSourcing
cd employeeservice
cd employeeservice

-- install
mvn clean install

mvn clean install -DskipTests

- update pom.xml
mvn clean install - U

-- chay bang maven
mvn spring-boot:run

- xoa file build cu va build lai moi
mvn clean spring-boot:run

- run check loi
mvn spring-boot:run -e

-- serverlink spring

--- axon server
cmd tai file ->
java -jar axonserver.jar

http://localhost:8024/

--- DB H2 book server
http://devbi.mobiwork.vn:9001/h2-console

--- DB H2 employee server
http://devbi.mobiwork.vn:9002/h2-console

--- axonserver voi docker
docker run -d --name axonserver -p 8024:8024 -p 8124:8124 axoniq/axonserver
- start voi th da dc khoi tao truoc do
docker start axonserver


--- lenh check folder, file trong du an, xem chuan Seo k
Get-ChildItem -Recurse | Where-Object { $_.FullName -notmatch '\\node_modules|\\.next|\\.git' } | ForEach-Object {
    $indent = "  " * ($_.FullName.Split('\').Count - (Get-Item .).FullName.Split('\').Count)
    if ($_.PSIsContainer) {
        "$indent📂 $($_.Name)"
    } else {
        "$indent📄 $($_.Name)"
    }
}


